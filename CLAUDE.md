# CLAUDE.md — ai-issues 작업 지침

## 프로젝트 한 줄 정의

AI 소식(모델·서비스 출시, 영상·이미지 모델, 하드웨어, 기업·자본)을 수집해서 "지금 많이 언급되는 이슈"를 순위로 보여주는 웹사이트이다. 사용자는 아무것도 설정하지 않아도 첫 화면과 순위가 맞아야 한다.

## 폴더 구조

```
/scripts/            수집·묶기·판정·순위 Python
/data/               items.json, issues.json (실행 결과)
/web/                index.html (GitHub Pages 진입점)
/.github/workflows/  15분 주기 cron
/wiki/               위키 SSOT
CLAUDE.md            이 파일
```

## 위키 SSOT 원칙

1. 위키는 현재 기준 사실만 담는다. 옛 결정, 폐기한 구조, 진행 중인 상태는 본문에 두지 않는다.
2. 코드 변경과 위키 변경은 같은 커밋에 함께 넣는다. 한쪽만 바뀌면 안 된다.
3. 위키는 누가 읽어도 정확히 파악할 수 있는 공식 기록이다. 은유와 비유 어휘를 쓰지 않는다. "기록 / 작성 / 반영 / 저장 / 추가 / 변경" 같은 정확한 동사만 쓴다.
4. 주어·목적어·동사를 살린 완성 문장으로 쓴다. 명사형 종결로 압축하지 않는다.
5. 해결한 이슈는 `wiki/archive/issues/`로 옮긴다. 폐기한 결정은 `wiki/archive/decisions/`로 옮긴다.

## 위키 페이지 구성

현재 작성한 페이지와 앞으로 작성할 페이지는 `wiki/README.md`의 인덱스에 기록한다. 페이지는 필요한 Phase에서 만들고, 만들 때 내용을 채운다. 제목만 있는 빈 파일은 만들지 않는다.

## 작업 방식

1. 계획을 말로 먼저 설명하고 완의 승인을 받은 뒤 구현한다.
2. Phase가 끝날 때마다 멈춰서 검증을 받는다.
3. 요청하지 않은 변경은 하지 않는다. 필요하면 먼저 말한다.
4. 한 커밋에 한 가지 작업 단위를 담는다. 예상 밖의 diff가 생기면 멈추고 보고한다. 스스로 고치지 않는다.
5. 결정이 필요하면 선택지를 최대 3개 제시하고 권고 1개를 붙인다.
6. 완료 보고는 항상 하나의 코드블록에 모두 담는다. 변경 파일 목록과 커밋 해시, 검증 결과(항목별 OK / NG), 발견한 문제와 결정이 필요한 사항, 위키 반영 위치를 적는다.

## 금지 어휘

응답과 위키 모두에서 다음 어휘를 쓰지 않는다. 보내기 전에 grep으로 확인한다.

- 박는다 / 박힘 / 박음 / 박혀있다 / 박제 (모든 활용형)
- 본질 / 정합 / 패러다임 / 큰 그림

## 언어

화면과 요약은 한국어로 쓴다. 출처 원문은 영어가 대부분이다.
